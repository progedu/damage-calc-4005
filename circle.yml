machine:
  node:
    version: 6.10.0
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/mocha
    - MOCHA_FILE=$CIRCLE\TEST_REPORTS/mocha/junit.xml node_modules/.bin/mocha test --reporter mocha-junit-reporter